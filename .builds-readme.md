# 📦 Callisto Builds

This directory contains production builds of Callisto for different platforms.

## 📁 Directory Structure

```
builds/
├── macos/
│   ├── v0.1.0/           # macOS builds for version 0.1.0
│   │   ├── *.dmg         # Disk image installer
│   │   ├── *.app         # Application bundle
│   │   └── BUILD_INFO.txt
│   └── v0.1.1/           # Next version...
├── linux/
│   ├── v0.1.0/           # Linux builds for version 0.1.0
│   │   ├── *.deb         # Debian/Ubuntu package
│   │   ├── *.rpm         # Fedora/RHEL package
│   │   ├── *.AppImage    # Universal Linux binary
│   │   └── BUILD_INFO.txt
│   └── v0.1.1/
├── windows/
│   ├── v0.1.0/           # Windows builds for version 0.1.0
│   │   ├── *.msi         # MSI installer
│   │   ├── *.exe         # NSIS installer
│   │   └── BUILD_INFO.txt
│   └── v0.1.1/
└── latest/               # Symbolic links or copies of latest builds
```

## 🚀 Installation

### macOS
```bash
# Open DMG file
open Callisto_0.1.0_aarch64.dmg

# Or drag .app to Applications folder
```

### Linux

**Debian/Ubuntu:**
```bash
sudo dpkg -i callisto_0.1.0_amd64.deb
```

**Fedora/RHEL:**
```bash
sudo rpm -i callisto-0.1.0-1.x86_64.rpm
```

**AppImage:**
```bash
chmod +x callisto_0.1.0_amd64.AppImage
./callisto_0.1.0_amd64.AppImage
```

### Windows
Double-click the `.msi` or `.exe` installer

## 📊 Build Information

Each version directory contains a `BUILD_INFO.txt` file with:
- Version number
- Build date and time
- Platform details
- Git commit hash (if available)
- Git branch (if available)

## 🔐 Verification

For security, always verify checksums before installation:

```bash
# Generate checksums
shasum -a 256 macos/v0.1.0/* > checksums.txt

# Verify
shasum -a 256 -c checksums.txt
```

## 📝 Release Notes

See [CHANGELOG.md](../CHANGELOG.md) for version history and changes.

## 🐛 Issues

If you encounter issues with a build:
1. Check the version in BUILD_INFO.txt
2. Verify the checksum
3. Try a clean installation
4. Report issues at: https://github.com/yourusername/callisto/issues

---

**Note:** This directory is automatically generated by the build scripts and should not be committed to version control.

